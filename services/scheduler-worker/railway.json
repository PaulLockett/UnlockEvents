{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm --filter @unlock-events/scheduler-worker build",
    "watchPatterns": [
      "/services/scheduler-worker/**",
      "/packages/scheduler/**",
      "/packages/temporal-worker-utils/**"
    ]
  },
  "deploy": {
    "startCommand": "pnpm --filter @unlock-events/scheduler-worker start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
