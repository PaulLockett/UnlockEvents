{
  "name": "@unlock-events/ai-gateway-worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "U2: AI Gateway activity worker",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup src/index.ts --format esm",
    "start": "node dist/index.js",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@temporalio/worker": "^1.11.0",
    "@temporalio/activity": "^1.11.0",
    "@unlock-events/temporal-worker-utils": "workspace:*",
    "@unlock-events/ai-gateway": "workspace:*",
    "@unlock-events/temporal-interfaces": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "tsup": "^8.3.0",
    "typescript": "^5.7.3"
  }
}
