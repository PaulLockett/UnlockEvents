// M1: Ingestion Manager workflows will be defined here.
// Each workflow orchestrates the crawl lifecycle for a source:
//   BeginExperiment → SubmitForAnalysis → AcceptVerdict → RecordOutcome → AdvancePhase

export async function placeholder(): Promise<string> {
  return "workflow stub - not yet implemented";
}
