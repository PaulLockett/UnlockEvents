{
  "name": "@unlock-events/extraction-engine-worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "E2: Extraction Engine child workflow worker",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup src/index.ts --format esm",
    "start": "node dist/index.js",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@temporalio/worker": "^1.11.0",
    "@temporalio/workflow": "^1.11.0",
    "@temporalio/activity": "^1.11.0",
    "@unlock-events/temporal-worker-utils": "workspace:*",
    "@unlock-events/temporal-interfaces": "workspace:*",
    "@unlock-events/extraction-engine": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "tsup": "^8.3.0",
    "typescript": "^5.7.3"
  }
}
